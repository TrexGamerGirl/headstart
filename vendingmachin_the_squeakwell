itmA = 1.20
itmB = 0.60
itmC = 0.75
print("welcome to the vending machine")
item = input("What do you wish to purchase? Item A, B or C input 1 for A, 2 for B and 3 for C ")
if item == 1:
    cost = itmA 
    mony = float(input("how much money did you put into the machine? "))
    if mony == cost:
        print("you have paid for the " + item)

    if mony >= cost:
        print(f"You have recieved {mony - cost} change thank you for this transaction.")

    if mony <= cost:
        print(f"You don't have enough money to purchase {item} you need {cost - mony} more funds")

if item == 2:
    cost = itmB 
    mony = float(input("how much money did you put into the machine? "))
    if mony == cost:
        print("you have paid for the " + item)

    if mony >= cost:
        print(f"You have recieved {mony - cost} change thank you for this transaction.")

    if mony <= cost:
        print(f"You don't have enough money to purchase {item} you need {cost - mony} more funds")

if item == 3:
    cost = itmC
    mony = float(input("how much money did you put into the machine? "))
    if mony == cost:
        print("you have paid for the " + item)

    if mony >= cost:
        print(f"You have recieved {mony - cost} change thank you for this transaction.")

    if mony <= cost:
        print(f"You don't have enough money to purchase {item} you need {cost - mony} more funds")